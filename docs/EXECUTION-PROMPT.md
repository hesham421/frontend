# AVELYNQ ERP — UI/UX Execution Prompt
## Ready-to-paste prompt for Claude Code (or AI agent)

```
Generated by : UI/UX Design Engine (Project 2.5)
Based on     : flow-diagram.md v2.0 + ui-ux-spec.md v2.0
Modules      : SEC · ORG · FILE · NOTIF (18 screens)
Stack        : AVELYNQ ERP Dashboard (React 18 · TypeScript 5.9 · Zustand 5)
Usage        : Paste this prompt into Claude Code. Run one module at a time
               using the per-module variants at the bottom of this file.
```

---

## ══ MASTER PROMPT (all 4 modules) ══

```
You are an expert Frontend Architect and UI/UX Engineer building the AVELYNQ ERP Dashboard.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ARCHITECTURAL RULES — READ BEFORE WRITING ANY CODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. STACK (mandatory — no deviation):
   React 18 · TypeScript 5.9 · Zustand 5 · Vite · @tabler/icons-webfont
   CSS design tokens (Vanilla CSS — NO Tailwind)

2. COMPONENT REUSE (strict — never write raw HTML elements):
   Always import and compose from src/components/ui/:
   - Button, IconButton                          → src/components/ui/Button.tsx
   - Input, Select, Checkbox, Switch             → src/components/ui/FormControls.tsx
   - Card, Stat, Badge, Avatar                   → src/components/ui/DataDisplay.tsx
   - Tabs, Breadcrumb, Alert, EmptyState,
     Dialog, Drawer                              → src/components/ui/OverlaysAndFeedback.tsx

3. DESIGN TOKENS (mandatory CSS variables):
   --brand-primary, --brand-accent, --surface-page, --surface-card,
   --surface-sunken, --border-default, --border-subtle,
   --shadow-xs/sm/md/lg, --radius-sm/md/lg/full,
   --control-sm (32px) / --control-md (38px) / --control-lg (44px)
   Colors: --navy-950/900/850, --green-500, --amber-500, --red-500, --blue-500

4. RTL SUPPORT (mandatory — use CSS logical properties only):
   margin-inline-start / margin-inline-end
   padding-inline-start / padding-inline-end
   border-inline-start / border-inline-end
   inset-inline-start / inset-inline-end
   text-align: start
   NEVER use: margin-left/right, padding-left/right, left/right, text-align: left

5. INTERNATIONALIZATION:
   - const { t, lang } = useLanguage(); in every component
   - Register ALL new UI text under both translations.en and translations.ar
     in src/stores/useLanguageStore.ts
   - Bilingual name fields (nameEn + nameAr) appear side-by-side or in Tabs

6. STATE MANAGEMENT:
   - Zustand stores in src/stores/ → lists, filters, selected entity, modal flags, mutations
   - useState ONLY for ephemeral UI state (open/close, local input value before submit)

7. NAVIGATION:
   - New screen keys → src/stores/useNavigationStore.ts (ScreenType union)
   - Page rendering → src/App.tsx
   - Sidebar links → src/layout/Sidebar.tsx (under correct section grouping)

8. LAYOUT HIERARCHY (every new screen must follow this order):
   Step 1 → Header Bar: Breadcrumb + h1 + primary [+ New] Button
   Step 2 → KPI Row: 3-4 Stat cards (total · active · relevant metric)
   Step 3 → Filter Bar: Input (search) + Select filters
   Step 4 → Data Grid: Card variant="flat" + table + Badge status
   Step 5 → Drawer: create/edit form (Drawer preferred for forms > 5 fields)
   Step 6 → Dialog: delete / deactivation confirmation
   Step 7 → EmptyState: when list empty or search returns nothing

9. BUSINESS RULES (non-negotiable):
   - Auto-generated codes (branch_code, dept_code, etc.) are HIDDEN in Create,
     READ-ONLY in Edit, and NEVER editable after creation
   - Deactivation is SOFT (isActiveFl = false) — not hard delete
   - LOV dropdowns show ACTIVE records only (is_active_fl=1) by default
   - In Edit mode: show current value even if inactive, with Badge "Inactive"
   - Both nameEn AND nameAr are required wherever the SRS mandates bilingual

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FEATURE ASSIGNMENT — ALL FOUR MODULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Implement the following 18 screens across 4 modules.
Follow the 5-stage execution plan (defined below) for EACH module.

────────────────────────────────────────────────────
MODULE 1: SECURITY (SEC) — 7 screens
────────────────────────────────────────────────────

SCR-SEC-001 — Authentication Shell (extend existing Login.tsx)
  • 5 linked public forms: Login · Signup · Activate · ForgotPassword · ResetPassword
  • Tabs variant="underline" to switch between Login and Create Account
  • No Navigation Guard — fully public
  • On login success: useAuthStore.login() → useNavigationStore.setCurrentScreen('dashboard')
  • Sub-form fields: see ui-ux-spec.md SCR-SEC-001

SCR-SEC-002 — User Management (new: src/pages/Security/Users.tsx)
  • PATTERN-1: Search + Create/Edit Dialog
  • Filters: username (text) · enabled (Select)
  • Table: username · email · enabled Badge · roles count Badge
  • Dialog: username* · email · password* (hidden in Edit) · enabled Switch · roles multi-Select
  • From Edit Dialog: "User Profile →" button → Drawer (SCR-SEC-006)
  • From Edit Dialog: "Data Scope →" button → Drawer (SCR-SEC-007)
  • Permission gate: PERM_USER_VIEW

SCR-SEC-003 — Role & RBAC Management (new: src/pages/Security/Roles.tsx)
  • PATTERN-1: Search + Edit Dialog (Dialog contains embedded Permission Matrix)
  • Filter: roleName text only
  • Table: roleCode Badge · roleName · IS_ACTIVE Badge
  • Dialog: roleCode* (Read-Only post-create) · roleName* · description · IS_ACTIVE Switch
  • Permission Matrix inside Dialog: Page × VIEW/CREATE/UPDATE/DELETE checkboxes
    - VIEW auto-checked on page add
    - Buttons: [+ Add Page] [Sync All] [Copy From Role]
  • Separate Activate / Deactivate buttons (not a toggle — per 2026-08-23 correction)
  • "Branch Data Scope →" → Drawer (SCR-SEC-007)
  • Permission gate: PERM_ROLE_VIEW

SCR-SEC-004 — Permission Registry (new: src/pages/Security/Permissions.tsx)
  • PATTERN-2: inline table + Dialog
  • Filters: name (text) · module (text)
  • Table: name · permissionType Badge · page
  • Dialog: name* (hint: PERM_<CODE>_<TYPE>) · permissionType Select · page Select (optional)
  • No Delete button (endpoint unconfirmed AS-IS)
  • Permission gate: PERM_PERMISSION_VIEW

SCR-SEC-005 — Page Registry (new: src/pages/Security/Pages.tsx)
  • PATTERN-1: Search + Entry (FLAT list — no tree view, OQ-013 final)
  • Filters: pageCode · module · active Select
  • Table: pageCode Badge · nameEn · module Badge · route mono · active Badge
  • Drawer (large form): pageCode* (auto-uppercase) · nameEn* · nameAr* · route* ·
    icon · module · parentId Select (self-ref) · displayOrder · description
  • Save auto-generates 4 permissions (note in UI: "Saving will auto-create VIEW/CREATE/UPDATE/DELETE permissions")
  • Deactivate / Reactivate as separate buttons
  • Permission gate: PERM_PAGE_VIEW

SCR-SEC-006 — User Profile (Drawer — opened from SCR-SEC-002)
  • PATTERN-2: Drawer
  • Fields: branchIdFk Select* (cross-module, active only) · fullNameEn · fullNameAr ·
    preferredLang Input (free text) · employeeIdFk Input (no FK) · isActiveFl Switch
  • NO Delete button (intentional exception per RULE-SEC-047)
  • Permission gate: PERM_USER_PROFILE_VIEW

SCR-SEC-007 — Role Data Scope — Branch Assignment (Drawer — opened from SCR-SEC-003)
  • PATTERN-2: Drawer
  • Fields: roleIdFk Select* · branchIdFk Select* (cross-module) ·
    dataAccessLevel Select* (Branch Only / Branch & Children / All) · isActiveFl Switch
  • Reuses PERM_ROLE_* — no new permissions
  • Delete button present (PERM_ROLE_UPDATE)

────────────────────────────────────────────────────
MODULE 2: ORGANIZATION (ORG) — 7 screens
────────────────────────────────────────────────────

SCR-ORG-001 — Legal Entities (new: src/pages/Organization/LegalEntities.tsx)
  • PATTERN-1: Search + Drawer
  • Filters: legal_entity_code · name_en · entity_type_id Select · is_active_fl Select
  • Table: code Badge · nameEn · nameAr · entityType Badge · status Badge
  • Drawer: nameEn* · nameAr* · entityTypeId* Select (Head Office/Branch Office/Subsidiary/Representative Office) · notes
  • Code auto-generated: hidden in Create, Read-Only in Edit
  • Deactivate blocked if active branches exist → Alert variant="warning"
  • Context link from row: [View Branches →] → SCR-ORG-002 filtered by this entity

SCR-ORG-002 — Branches (new: src/pages/Organization/Branches.tsx)
  • PATTERN-1: Search + Drawer
  • Filters: branch_code · name_en · legal_entity_fk Select · branch_type_id Select · is_active_fl
  • Drawer: nameEn* · nameAr* · legalEntityFk* Select (active only) ·
    branchTypeId* Select (Main/Sub/Operations/Admin) · notes
  • Deactivate cascade check: Departments | CostCenters | LocationSites active?
  • Context links in row: [Departments] [Cost Centers] [Location Sites]

SCR-ORG-003 — Regions (new: src/pages/Organization/Regions.tsx)
  • PATTERN-1: Search + Drawer
  • Filters: region_code · name_en · legal_entity_fk · region_type_id_fk · is_active_fl
  • Drawer: nameEn* · nameAr* · legalEntityFk* · regionTypeIdFk* (active only, read-only source) · notes
  • ⚠ OQ-ORG-002: regionTypeIdFk rendered as a regular Select LOV (no admin screen for types yet)

SCR-ORG-004 — Departments (new: src/pages/Organization/Departments.tsx)
  • PATTERN-3: Specialized Tree Hierarchy — reuse Organization.tsx tree pattern
  • Branch filter is REQUIRED before tree renders → Alert variant="info" if not selected
  • Two-column layout: Tree Panel (left) + Entry Panel (right)
  • Tree: expandable nodes, Summary Badge variant="warning", per-node [+ Child] [Edit] [Deactivate]
  • Entry Panel: nameEn* · nameAr* · branchFk* · parentDepartmentFk (auto-fill from tree) ·
    nodeTypeId* Select (Summary/Detail, locked after save per RULE-ORG-020) · notes
  • [+ Root Department] button at top of Tree Panel

SCR-ORG-005 — Cost Centers (new: src/pages/Organization/CostCenters.tsx)
  • PATTERN-3: Specialized Tree — identical to SCR-ORG-004
  • Extra filter: cost_center_type_id Select (Direct/Indirect/Shared)
  • Extra entry field: costCenterTypeId* Select (Direct/Indirect/Shared)

SCR-ORG-006 — Profit Centers (new: src/pages/Organization/ProfitCenters.tsx)
  • PATTERN-1: Search + Drawer
  • Filters: profit_center_code · name_en · legal_entity_fk · is_active_fl
  • Drawer: nameEn* · nameAr* · legalEntityFk* · notes

SCR-ORG-007 — Location Sites (new: src/pages/Organization/LocationSites.tsx)
  • PATTERN-1: Search + Drawer
  • Filters: location_site_code · name_en · branch_fk · site_type_id · is_active_fl
  • Drawer: nameEn* · nameAr* · branchFk* (active only) ·
    siteTypeId* Select (Office/Warehouse/Factory/Site/Retail) · notes

────────────────────────────────────────────────────
MODULE 3: FILE SERVICE (FILE) — 1 shared component
────────────────────────────────────────────────────

SCR-FILE-001 — File Attachment Panel (new: src/components/features/FileAttachmentPanel.tsx)
  • PATTERN-2: Inline Panel (embeddable widget — NOT a standalone page)
  • Props: { ownerId: string, ownerType: string, moduleCode: string }
  • File list: fileName · category · type Badge · size · date · [Download ⬇] [Delete 🗑]
  • Upload: fileCategoryFk Select (filtered by moduleCode) + drag-and-drop file picker
  • 2-step upload flow: POST /upload-token → POST /upload/{token}
  • Progress bar during upload
  • Delete: confirmation Dialog → DELETE with token
  • Error states: 403 → Alert "You don't own this file" | size > 5MB → pre-upload Alert
  • Empty state: EmptyState icon="ti-paperclip"

────────────────────────────────────────────────────
MODULE 4: NOTIFICATION SERVICE (NOTIF) — 3 screens
────────────────────────────────────────────────────

SCR-NOTIF-001 — Notification Bell & History
  Bell → extend existing topbar notification icon (add unread Badge overlay + dropdown panel)
  History → new: src/pages/Notifications/NotificationInbox.tsx
  • Bell Dropdown (Card variant="raised"): unread counter · subject · type Badge · time ·
    [Mark All Read] · [View All →]
  • History Page: PATTERN-1 filters (type · status · date range) + table with mark-read per row
  • Every authenticated user — each sees own notifications only
  • currentScreen key: 'notifications-inbox'

SCR-NOTIF-002 — Notification Templates (new: src/pages/Notifications/NotificationTemplates.tsx)
  • PATTERN-1: Search + Drawer (width="lg")
  • Filters: templateCode · channelTypeId · moduleCode · isActiveFl
  • Table: templateCode Badge · templateNameEn · channel Badge · module Badge · active Badge
  • Drawer: templateCode* (immutable post-create) · templateNameEn* · templateNameAr* ·
    channelTypeId* Select (Email/SMS/WhatsApp/Push/Internal) · moduleCode* ·
    templateBodyEn* textarea (Placeholder-aware) · templateBodyAr* textarea ·
    isActiveFl Switch
  • Tabs variant="underline" inside Drawer to switch between EN body and AR body
  • Alert variant="info" "Both English and Arabic bodies are required" above body tabs
  • Deactivate action on row

SCR-NOTIF-003 — Channel Configuration (new: src/pages/Notifications/NotificationChannels.tsx)
  • PATTERN-2: Inline toggle list (5 fixed rows — no add/delete)
  • Each row: channel name (Read-Only) · isEnabledFl Switch · configJson preview
  • [Edit Config] per row → inline JSON editor opens below that row
  • [Save Changes] → PUT per modified row
  • Alert variant="warning" when any channel Switch is turned OFF

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DELIVERABLES — 5-STAGE EXECUTION PLAN (per module)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For each module (SEC, ORG, FILE, NOTIF), deliver in this exact order:

STAGE 1 — Domain Models & Mock Data (src/data/mockData.ts)
  • TypeScript interfaces with strict types
  • Realistic bilingual mock items (nameEn, nameAr, codes, statuses)
  • Enums for status values, entity types, LOVs

STAGE 2 — Zustand Store (src/stores/use<Feature>Store.ts)
  State slices:
  - entities: T[] (loaded from mock data)
  - filters: { search: string, status: string, ...module-specific }
  - selectedEntity: T | null (for Drawer inspection / edit)
  - isDrawerOpen: boolean
  - isDialogOpen: boolean (delete/confirm dialogs)
  Actions:
  - setFilter(key, value)
  - setSelectedEntity(entity | null)
  - openDrawer() / closeDrawer()
  - addEntity(entity) / updateEntity(id, patch) / deactivateEntity(id)

STAGE 3 — Bilingual Translation Keys (src/stores/useLanguageStore.ts)
  • 100% parity between translations.en and translations.ar
  • Keys for: page title · column headers · button labels · filter labels ·
    empty state titles/descriptions · error messages · confirmation messages

STAGE 4 — Screen Component (src/pages/<Module>/<ScreenName>.tsx)
  Layout order (mandatory):
  1. Header Bar: <Breadcrumb> + <h1> + <Button variant="primary"> [+ New]
  2. KPI Row: 3-4 <Stat> cards
  3. Filter Bar: <Input> search + <Select> filters
  4. Data Grid: <Card variant="flat"> → table rows with <Badge> status
  5. <Drawer>: create/edit form with all SRS B3 fields
  6. <Dialog>: delete/deactivate confirmation
  7. <EmptyState>: when no records

STAGE 5 — Navigation Wiring
  a) src/stores/useNavigationStore.ts → add new ScreenType key(s)
  b) src/App.tsx → add new screen case in the router
  c) src/layout/Sidebar.tsx → add menu item under correct section:
       - SEC screens → "Security" section
       - ORG screens → "Organization" section
       - NOTIF screens → "Notification Settings" section (Admin) + Bell always visible
       - FILE → no sidebar item (embedded widget)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL CONSTRAINTS — ALWAYS ENFORCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Use existing components — never write raw <button>, <input>, <select>
✅ Use CSS tokens — never hardcode colors, sizes, or shadows
✅ RTL via logical CSS properties — never use directional properties
✅ Register bilingual keys — never use hardcoded English strings in JSX
✅ Soft deactivation — never hard-delete ORG entities
✅ Business codes hidden in Create, Read-Only in Edit, never editable
✅ LOV dropdowns: active records only (with inactive-value display in Edit)
✅ Confirmation Dialogs before any destructive action
✅ EmptyState for all empty/no-result conditions
✅ Alert variant="warning" for all cascade-blocked deactivations

❌ Do NOT install new npm packages
❌ Do NOT use Tailwind classes
❌ Do NOT hardcode left/right CSS
❌ Do NOT hardcode English UI strings in JSX
❌ Do NOT create new primitive components (extend existing ones)
❌ Do NOT add a Delete button to SCR-SEC-006 (User Profile) — intentional
❌ Do NOT add a tree view to SCR-SEC-005 (Page Registry) — AS-IS flat list
```

---

## ══ PER-MODULE PROMPTS (run one at a time) ══

### 🔐 MODULE 1 ONLY — Security (SEC)

```
[Paste the MASTER PROMPT above, then append:]

SCOPE: Implement MODULE 1 — SECURITY only.
Screens: SCR-SEC-001 (extend Login.tsx) · SCR-SEC-002 · SCR-SEC-003 ·
         SCR-SEC-004 · SCR-SEC-005 · SCR-SEC-006 (Drawer from SEC-002) ·
         SCR-SEC-007 (Drawer from SEC-003)
Store: src/stores/useUsersStore.ts + src/stores/useRolesStore.ts
Pages: src/pages/Security/Users.tsx · Roles.tsx · Permissions.tsx · Pages.tsx
Sidebar section: "Security"
Do NOT implement ORG, FILE, or NOTIF in this run.
```

---

### 🏢 MODULE 2 ONLY — Organization (ORG)

```
[Paste the MASTER PROMPT above, then append:]

SCOPE: Implement MODULE 2 — ORGANIZATION only.
Screens: SCR-ORG-001 · SCR-ORG-002 · SCR-ORG-003 · SCR-ORG-004 (tree) ·
         SCR-ORG-005 (tree) · SCR-ORG-006 · SCR-ORG-007
Store: extend src/stores/useOrganizationStore.ts (already exists)
Pages: src/pages/Organization/LegalEntities.tsx · Branches.tsx · Regions.tsx ·
       Departments.tsx · CostCenters.tsx · ProfitCenters.tsx · LocationSites.tsx
Sidebar section: "Organization" (already has some items — add new ones)
Tree screens (SCR-ORG-004 / 005): reuse the tree interaction pattern from
       the existing Organization.tsx "Org Units Tree" tab for visual consistency
Do NOT implement SEC, FILE, or NOTIF in this run.
```

---

### 📎 MODULE 3 ONLY — File Service (FILE)

```
[Paste the MASTER PROMPT above, then append:]

SCOPE: Implement MODULE 3 — FILE SERVICE only.
Component: src/components/features/FileAttachmentPanel.tsx
           Props: { ownerId: string, ownerType: string, moduleCode: string }
           This is an EMBEDDED WIDGET — not a standalone page.
           No sidebar item. No screen route.
After creating the component, demonstrate its usage by embedding it
into the existing Accounts.tsx detail Drawer as a proof-of-concept:
  <FileAttachmentPanel ownerId={account.id} ownerType="ACCOUNT" moduleCode="FIN" />
Do NOT implement SEC, ORG, or NOTIF in this run.
```

---

### 🔔 MODULE 4 ONLY — Notification Service (NOTIF)

```
[Paste the MASTER PROMPT above, then append:]

SCOPE: Implement MODULE 4 — NOTIFICATION SERVICE only.
Screens:
  SCR-NOTIF-001 Bell: extend the existing topbar notification icon
    (add unread count Badge overlay, dropdown panel, "View All" link)
  SCR-NOTIF-001 History: src/pages/Notifications/NotificationInbox.tsx
  SCR-NOTIF-002: src/pages/Notifications/NotificationTemplates.tsx
  SCR-NOTIF-003: src/pages/Notifications/NotificationChannels.tsx
Store: src/stores/useNotificationsStore.ts (inbox) +
       src/stores/useNotificationTemplatesStore.ts (templates/channels)
Sidebar section: "Notification Settings" (Admin only) for Templates + Channels
Bell: always visible in topbar for all authenticated users
Do NOT implement SEC, ORG, or FILE in this run.
```

---

## ══ SINGLE-SCREEN PROMPTS (surgical updates) ══

### User Management only (SCR-SEC-002)

```
[Paste MASTER PROMPT, then:]
SCOPE: Implement SCR-SEC-002 (User Management) only.
File: src/pages/Security/Users.tsx
Store: src/stores/useUsersStore.ts
Include Drawer integration for SCR-SEC-006 (User Profile) and
SCR-SEC-007 (Data Scope) — these open as nested Drawers from the Edit Dialog.
```

### Department Tree only (SCR-ORG-004)

```
[Paste MASTER PROMPT, then:]
SCOPE: Implement SCR-ORG-004 (Departments — Tree Hierarchy) only.
File: src/pages/Organization/Departments.tsx
Store: extend useOrganizationStore.ts with department tree state
Reuse the Org Units Tree pattern from Organization.tsx for visual consistency.
The branch filter is REQUIRED — show Alert variant="info" if not selected.
Two-column layout: Tree Panel (left) + Entry Panel (right).
nodeTypeId is LOCKED after first save (disable the Select in Edit when nodeTypeId was already set).
```

### Notification Bell only (SCR-NOTIF-001)

```
[Paste MASTER PROMPT, then:]
SCOPE: Implement SCR-NOTIF-001 (Notification Bell + History) only.
Bell: Extend existing topbar notification icon — add:
  - Unread count Badge variant="danger" overlay
  - Dropdown panel (Card variant="raised") with last 5-10 notifications
  - [Mark All Read] Button variant="ghost"
  - [View All →] Button → sets currentScreen = 'notifications-inbox'
History page: src/pages/Notifications/NotificationInbox.tsx
Store: src/stores/useNotificationsStore.ts
```

### File Attachment Panel only (SCR-FILE-001)

```
[Paste MASTER PROMPT, then:]
SCOPE: Implement SCR-FILE-001 (File Attachment Panel) only.
File: src/components/features/FileAttachmentPanel.tsx
Props: { ownerId: string, ownerType: string, moduleCode: string }
This is an embedded widget — no routing, no sidebar.
Demonstrate by embedding into Accounts.tsx detail Drawer:
  <FileAttachmentPanel ownerId={selectedAccount.id} ownerType="ACCOUNT" moduleCode="FIN" />
```

---

## ══ VERIFICATION CHECKLIST (run after each module) ══

After each module implementation, verify:

```
□ Every UI string goes through t() — no hardcoded English in JSX
□ Both translations.en and translations.ar have matching keys (100% parity)
□ All spacing/positioning uses CSS logical properties (no left/right/margin-left)
□ Every new Button, Input, Select, Badge, Dialog, Drawer uses the primitives
□ No new npm packages were installed
□ No Tailwind classes appear in any new file
□ Business codes hidden in Create, Read-Only in Edit
□ Deactivate shows confirmation Dialog before action
□ Cascade-blocked deactivations show Alert variant="warning"
□ EmptyState renders when list is empty (not blank white space)
□ Zustand store covers: list · filters · selected · drawer · dialog flags
□ Navigation store updated with new screen key(s)
□ App.tsx routes new screen(s)
□ Sidebar.tsx shows new item under correct section
□ SCR-SEC-006 has NO Delete button
□ SCR-SEC-005 has NO tree view (flat list only)
□ SCR-ORG-004/005 tree requires branch filter before rendering
□ FILE panel props: ownerId · ownerType · moduleCode (not user-entered)
□ NOTIF-002 Drawer requires both templateBodyEn AND templateBodyAr
□ NOTIF-003 shows 5 fixed rows (no add/delete)
```

---

```
Generated: 2026-08-23
Based on: flow-diagram.md v2.0 + ui-ux-spec.md v2.0
Source governance: SRS B1-B4 (SEC · ORG · FILE · NOTIF)
This prompt is the execution artifact — paste into Claude Code to build.
```
