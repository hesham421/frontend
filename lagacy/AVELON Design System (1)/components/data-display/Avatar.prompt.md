User/entity avatar. Shows an image when `src` is given, otherwise deterministic initials with a brand-family tint.

```jsx
<Avatar name="Layla Hassan" size="md" />
<Avatar name="AVELYNQ" square src="/assets/avelynq-appicon.png" />
```

Sizes `xs` `sm` `md` `lg`. `square` for app/entity tiles.
