Toggle switch for binary settings (feature flags, activation, preferences).

```jsx
<Switch label="Active" checked={v} onChange={e=>setV(e.target.checked)} />
```

Use a Checkbox instead inside data tables and multi-select lists; reserve Switch for immediate-effect settings.
