Breadcrumb trail showing the record's place in the module hierarchy.

```jsx
<Breadcrumb items={['Finance','General Ledger',{label:'Chart of Accounts'}]} />
```

Last item renders as the current page (bold). Items accept strings or `{label,href}`.
